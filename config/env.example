# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=whatsanalyze
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# DuckDB Configuration
DUCKDB_PATH=./data/whatsanalyze.db

# Note: Copy this file to .env in the root directory and update the values as needed
# Make sure to keep your actual .env file out of version control
